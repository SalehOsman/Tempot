---
title: إدارة النسخ الاحتياطي
description: طريقة إنشاء النسخ الاحتياطية وفحصها واسترجاعها من داخل البوت
tags:
  - user-guide
  - backup
  - restore
audience:
  - operator
  - super-admin
contentType: user-guide
difficulty: intermediate
---

## نظرة عامة

إدارة النسخ الاحتياطي من صلاحيات المدير العام فقط. الهدف منها حماية قاعدة
البيانات وملفات التخزين المحلية عبر إنشاء ملفات نسخ احتياطية مشفرة داخل مجلد
النسخ الاحتياطي المحلي للمشروع.

## إنشاء نسخة احتياطية

لإنشاء نسخة احتياطية من داخل البوت:

1. افتح القائمة الرئيسية باستخدام `/start`.
2. افتح قائمة النسخ الاحتياطي.
3. اضغط إنشاء نسخة احتياطية.
4. انتظر رسالة اكتمال العملية.

تعرض رسالة النجاح مرجع النسخة واسم الملف. يتم حفظ ملفات النسخ محليًا داخل مجلد
النسخ الاحتياطي المضبوط في المشروع. اسم الملف يحتوي على التاريخ والوقت لتسهيل
تحديد النسخة المطلوبة لاحقًا.

## عرض سجل النسخ

استخدم سجل النسخ لعرض أحدث عمليات النسخ الاحتياطي. يجب مراجعة السجل قبل أي
استرجاع لمعرفة مرجع النسخة وتاريخ إنشائها.

## استرجاع نسخة محفوظة

لاسترجاع نسخة محفوظة:

1. افتح قائمة النسخ الاحتياطي.
2. اضغط استرجاع نسخة محفوظة.
3. اختر النسخة المطلوبة.
4. أكد تحذير الاسترجاع.
5. أكمل خطوة التأكيد النهائية.

الاسترجاع الفعلي عملية تشغيلية مؤثرة وقد تستبدل حالة قاعدة البيانات الحالية.
لذلك لا يتم تنفيذها إلا بواسطة المدير العام وبعد اختيار النسخة المطلوبة بوضوح.

## فحص الاسترجاع

فحص الاسترجاع يتحقق من أن النسخة قابلة للقراءة والتحقق بدون استبدال قاعدة
البيانات الحالية. يفضل تشغيله قبل أي استرجاع فعلي.

## إعادة التهيئة

إعادة التهيئة تعيد قاعدة البيانات إلى حالة أولية. يتم حذف المستخدمين الحاليين
والاعتماد على `SUPER_ADMIN_IDS` لإعادة إنشاء حساب المدير العام عند التشغيل أو
البووتستراب التالي. لا تستخدم هذا الخيار إلا عند الحاجة لحالة محلية نظيفة.
