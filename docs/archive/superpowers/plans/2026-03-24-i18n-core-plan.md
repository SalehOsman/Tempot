# i18n Core Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Establish the foundational i18n-core package providing multi-language support (Arabic primary) and enforcing the "i18n-Only Rule" (Rule XXXIX).

**Architecture:** A wrapper around `i18next` that integrates with `@tempot/session-manager` (via `AsyncLocalStorage`) to automatically detect user language from the current session. Validates locale files via Zod and uses an ESLint-based AST check for hardcoded strings.

**Tech Stack:** TypeScript, i18next, glob, zod, neverthrow, sanitize-html.

---

### Task 1: Initialize Package Structure

**Files:**
- Create: `packages/i18n-core/package.json`
- Create: `packages/i18n-core/tsconfig.json`
- Create: `packages/i18n-core/vitest.config.ts`

- [ ] **Step 1: Create package.json**
Run: `mkdir -p packages/i18n-core && cd packages/i18n-core && pnpm init`
Modify package.json to include standard Tempot package metadata.

- [ ] **Step 2: Add dependencies**
Run: `cd packages/i18n-core && pnpm add i18next zod neverthrow sanitize-html glob`
Run: `cd packages/i18n-core && pnpm add -D vitest @types/sanitize-html typescript`

- [ ] **Step 3: Setup TS Config and Vitest**
Create `tsconfig.json` extending root, and `vitest.config.ts`.

- [ ] **Step 4: Commit**
Run: `git add packages/i18n-core/package.json packages/i18n-core/tsconfig.json packages/i18n-core/vitest.config.ts packages/i18n-core/pnpm-workspace.yaml` (if applicable)
Run: `git commit -m "chore: initialize i18n-core package structure"`

---

### Task 2: Translation Engine Configuration

**Files:**
- Create: `packages/i18n-core/src/i18n.config.ts`
- Test: `packages/i18n-core/tests/unit/i18n-config.test.ts`

- [ ] **Step 1: Write the failing test**
Create `tests/unit/i18n-config.test.ts` testing for `ar` primary and `en` fallback in `i18nConfig`.

- [ ] **Step 2: Run test to verify it fails**
Run: `cd packages/i18n-core && pnpm vitest run tests/unit/i18n-config.test.ts`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**
Create `src/i18n.config.ts` with `{ lng: 'ar', fallbackLng: 'en', supportedLngs: ['ar', 'en'] }`.

- [ ] **Step 4: Run test to verify it passes**
Run: `cd packages/i18n-core && pnpm vitest run tests/unit/i18n-config.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**
Run: `git add packages/i18n-core/src/i18n.config.ts packages/i18n-core/tests/unit/i18n-config.test.ts`
Run: `git commit -m "feat(i18n-core): configure i18next with Arabic as primary language"`

---

### Task 3: Context-Aware t() Function

**Files:**
- Create: `packages/i18n-core/src/t.ts`
- Test: `packages/i18n-core/tests/unit/t.test.ts`

- [ ] **Step 1: Write the failing test**
Create `tests/unit/t.test.ts` to mock `sessionContext` from `@tempot/session-manager` and test `t('key')`.

- [ ] **Step 2: Run test to verify it fails**
Run: `cd packages/i18n-core && pnpm vitest run tests/unit/t.test.ts`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**
Create `src/t.ts` importing `i18next` and `sessionContext`, returning `i18next.t(key, { lng: sessionContext.getStore()?.lang || 'ar' })`.

- [ ] **Step 4: Run test to verify it passes**
Run: `cd packages/i18n-core && pnpm vitest run tests/unit/t.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**
Run: `git add packages/i18n-core/src/t.ts packages/i18n-core/tests/unit/t.test.ts`
Run: `git commit -m "feat(i18n-core): implement context-aware t() function"`

---

### Task 4: Modular Locale Loader

**Files:**
- Create: `packages/i18n-core/src/loader.ts`
- Test: `packages/i18n-core/tests/unit/loader.test.ts`

- [ ] **Step 1: Write the failing test**
Create `tests/unit/loader.test.ts` to mock `glob` and file system, and verify `i18next.addResourceBundle` is called.

- [ ] **Step 2: Run test to verify it fails**
Run: `cd packages/i18n-core && pnpm vitest run tests/unit/loader.test.ts`
Expected: FAIL

- [ ] **Step 3: Write minimal implementation**
Create `src/loader.ts` that globs `modules/*/locales/*.json` and adds to `i18next`. Return `Result<void, Error>` using `neverthrow`.

- [ ] **Step 4: Run test to verify it passes**
Run: `cd packages/i18n-core && pnpm vitest run tests/unit/loader.test.ts`
Expected: PASS

- [ ] **Step 5: Commit**
Run: `git add packages/i18n-core/src/loader.ts packages/i18n-core/tests/unit/loader.test.ts`
Run: `git commit -m "feat(i18n-core): implement modular locale loader"`

---

### Task 5: Sanitization and Helpers

**Files:**
- Create: `packages/i18n-core/src/helpers.ts`
- Create: `packages/i18n-core/src/sanitizer.ts`
- Test: `packages/i18n-core/tests/unit/helpers.test.ts`

- [ ] **Step 1: Write the failing tests**
Create tests for `getLocaleInfo()` (returning rtl/ltr) and sanitization wrapper.

- [ ] **Step 2: Run test to verify it fails**
Run tests.

- [ ] **Step 3: Write minimal implementation**
Implement `getLocaleInfo` and `sanitizeOptions` using `sanitize-html`.

- [ ] **Step 4: Run test to verify it passes**
Run tests.

- [ ] **Step 5: Commit**
Commit changes.

---

### Task 6: Export Public API

**Files:**
- Create: `packages/i18n-core/src/index.ts`

- [ ] **Step 1: Export modules**
Export `t`, `loadModuleLocales`, `getLocaleInfo` from `index.ts`.

- [ ] **Step 2: Commit**
Commit `index.ts`.