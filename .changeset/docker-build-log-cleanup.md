---
"bot-server": patch
---

Build the Docker image through the bot runtime pipeline only and install builder-stage native build tools so bot-server image builds avoid docs output errors and optional binding failures.
