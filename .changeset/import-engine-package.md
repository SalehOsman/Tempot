---
"@tempot/import-engine": minor
---

Implement the import-engine package with typed import contracts, deterministic CSV/XLSX parsing, schema-adapter validation, queue workflow, batch events, document-engine error report requests, and lifecycle events.
