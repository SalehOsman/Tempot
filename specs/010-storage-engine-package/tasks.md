# Storage Engine — Task Breakdown

**Feature:** 010-storage-engine-package  
**Source:** spec.md (Clarified) + plan.md (Corrected)  
**Generated:** 2026-03-26

---

## Task 0: Package Scaffolding

**Priority:** P0 (prerequisite for all other tasks)  
**Estimated time:** 5 min  
**FR:** None (infrastructure)

**Files to create:**

- `packages/storage-engine/.gitignore`
- `packages/storage-engine/tsconfig.json`
- `packages/storage-engine/package.json`
- `packages/storage-engine/vitest.config.ts`
- `packages/storage-engine/src/index.ts` (empty barrel)
- `packages/storage-engine/tests/unit/` (directory)

**Test file:** N/A (infrastructure only — validated by 10-point checklist)

**Acceptance criteria:**

- [ ] All 10 points of `docs/developer/package-creation-checklist.md` pass
- [ ] `.gitignore` includes: `dist/`, `node_modules/`, `*.tsbuildinfo`, `src/**/*.js`, `src/**/*.js.map`, `src/**/*.d.ts`, `src/**/*.d.ts.map`, `tests/**/*.js`, `tests/**/*.d.ts`
- [ ] `tsconfig.json` extends `../../tsconfig.json`, has `"outDir": "dist"`, `"rootDir": "src"`
- [ ] `package.json` has `"type": "module"`, `"main": "dist/index.js"`, `"types": "dist/index.d.ts"`, `"exports": { ".": "./dist/index.js" }`
- [ ] `package.json` has exact versions: `vitest: "4.1.0"`, `typescript: "5.9.3"`, `neverthrow: "8.2.0"`
- [ ] Dependencies match plan.md: `@aws-sdk/client-s3`, `@aws-sdk/s3-request-presigner`, `@aws-sdk/lib-storage`, `@googleapis/drive`, `file-type`, `uuid`
- [ ] Workspace dependencies: `@tempot/shared`, `@tempot/database`, `@tempot/event-bus`, `@tempot/session-manager`, `@tempot/logger`
- [ ] `vitest.config.ts` matches existing package patterns
- [ ] `src/index.ts` exists as empty barrel file
- [ ] No compiled artifacts in `src/`

---

## Task 1: Type Definitions, Contracts & Error Codes

**Priority:** P0 (dependency for all service tasks)  
**Estimated time:** 10 min  
**FR:** FR-001, FR-004, FR-007

**Files to create:**

- `packages/storage-engine/src/types.ts`
- `packages/storage-engine/src/contracts.ts`
- `packages/storage-engine/src/errors.ts`

**Test file:** `packages/storage-engine/tests/unit/types.test.ts`

**Acceptance criteria:**

- [ ] `StorageProviderType` type exported: `'local' | 's3' | 'drive' | 'telegram'`
- [ ] `StorageConfig` interface exported with `provider`, `maxFileSize`, `allowedMimeTypes`, `local?`, `s3?`, `drive?`, `telegram?`, `retention?`
- [ ] `LocalProviderConfig` interface exported: `{ basePath: string }`
- [ ] `S3ProviderConfig` interface exported: `{ bucket, region, encryptionMode?, kmsKeyId? }`
- [ ] `DriveProviderConfig` interface exported: `{ folderId: string }`
- [ ] `TelegramProviderConfig` interface exported: `{ storageChatId: number | string }`
- [ ] `RetentionConfig` interface exported: `{ days: number; cronSchedule: string }`
- [ ] `UploadOptions` interface exported: `{ originalName, mimeType, size, moduleId?, entityId?, metadata? }`
- [ ] `ProviderUploadResult` interface exported: `{ providerKey: string; url?: string }`
- [ ] `Attachment` interface exported with ALL BaseEntity audit fields (`id`, `createdAt`, `updatedAt`, `createdBy?`, `updatedBy?`, `isDeleted`, `deletedAt?`, `deletedBy?`)
- [ ] `DEFAULT_STORAGE_CONFIG` constant exported with 10MB max, 9 MIME types, 30-day retention
- [ ] `VectorIndexer` interface exported (deferred D4): `index(attachmentId, content): AsyncResult<void, AppError>`
- [ ] `StorageProvider` interface exported with 5 methods: `upload`, `download`, `delete`, `getSignedUrl`, `exists` — all returning `AsyncResult<T, AppError>`
- [ ] `StorageFileUploadedPayload` interface exported with all fields from spec
- [ ] `StorageFileDeletedPayload` interface exported with `permanent: boolean` field
- [ ] `STORAGE_ERRORS` constant exported with hierarchical error codes (3 categories: provider, file, validation, rollback). Note: Attachment CRUD errors are auto-generated by BaseRepository — do NOT duplicate.
- [ ] `STORAGE_ERRORS` includes `HARD_DELETE_FAILED` and `EVENT_PUBLISH_FAILED` codes (from brainstorming findings)
- [ ] No `any` types
- [ ] All tests pass

---

## Task 2: File Validation Service

**Priority:** P1  
**Estimated time:** 15 min  
**FR:** FR-004, SC-004, Edge Cases (MIME spoofing)  
**Dependencies:** Task 0, Task 1

**Files to create:**

- `packages/storage-engine/src/validation.service.ts`

**Test file:** `packages/storage-engine/tests/unit/validation-service.test.ts`

**Acceptance criteria:**

- [ ] `ValidatedFile` interface exported: `{ sanitizedName: string; generatedFileName: string }`
- [ ] `ValidationService` class exported, takes `StorageConfig` in constructor
- [ ] `validateUpload(options: UploadOptions)` returns `Result<ValidatedFile, AppError>` (synchronous checks)
- [ ] `validateMimeType(data: Buffer, declaredMime: string)` returns `AsyncResult<void, AppError>` (magic byte check)
- [ ] `validateMimeType` uses `file-type` package (`fileTypeFromBuffer`) to detect actual MIME type
- [ ] `validateMimeType` returns `STORAGE_ERRORS.MIME_MISMATCH` when declared MIME does not match detected MIME
- [ ] `validateMimeType` skips check when `file-type` cannot detect (e.g., text/plain, text/csv)
- [ ] Rejects empty files (size <= 0) with `STORAGE_ERRORS.EMPTY_FILE`
- [ ] Rejects oversized files with `STORAGE_ERRORS.FILE_TOO_LARGE` including `size` and `maxSize` context
- [ ] Rejects disallowed MIME types with `STORAGE_ERRORS.MIME_NOT_ALLOWED` including `mimeType` and `allowed` context
- [ ] `sanitizeFileName()` strips path traversal via `path.basename()` then strips special chars via `/[^a-zA-Z0-9._-]/g` (SC-004)
- [ ] Rejects filenames that sanitize to empty string with `STORAGE_ERRORS.INVALID_FILENAME`
- [ ] Generates UUID v7 filename with original extension (D2)
- [ ] No `any` types
- [ ] All tests pass (minimum 10 tests: empty file, oversized, bad MIME, MIME mismatch via magic bytes, MIME match passes, undetectable MIME skips, path traversal, special chars, valid upload, extension extraction)

---

## Task 3: LocalProvider

**Priority:** P1  
**Estimated time:** 15 min  
**FR:** FR-001, FR-006  
**Dependencies:** Task 0, Task 1

**Files to create:**

- `packages/storage-engine/src/providers/local.provider.ts`

**Test file:** `packages/storage-engine/tests/unit/local-provider.test.ts`

**Acceptance criteria:**

- [ ] `LocalProvider` class implements `StorageProvider` interface
- [ ] `type` property is `'local'` (readonly)
- [ ] Constructor takes `LocalProviderConfig`
- [ ] `upload()` creates directories with `mkdir({ recursive: true })`, writes `Buffer` via `writeFile`, writes `Readable` via `pipeline` to `createWriteStream`
- [ ] `download()` returns `Readable` via `createReadStream` after checking file exists with `access()`
- [ ] `delete()` removes file via `unlink()`
- [ ] `getSignedUrl()` returns the local file path after checking file exists
- [ ] `exists()` returns `true`/`false` via `stat()` — never errors on missing file
- [ ] All methods return `AsyncResult<T, AppError>` — no thrown exceptions
- [ ] Uses native `node:fs/promises` and `node:fs` streams — NO `fs-extra`
- [ ] Error codes match `STORAGE_ERRORS` constants
- [ ] No `any` types
- [ ] All tests pass (use real temp directories, minimum 7 tests: upload buffer, upload stream, download, delete, exists true/false, getSignedUrl, not found)

---

## Task 4: S3Provider

**Priority:** P1  
**Estimated time:** 15 min  
**FR:** FR-001, FR-006, D5  
**Dependencies:** Task 0, Task 1

**Files to create:**

- `packages/storage-engine/src/providers/s3.provider.ts`

**Test file:** `packages/storage-engine/tests/unit/s3-provider.test.ts`

**Acceptance criteria:**

- [ ] `S3Provider` class implements `StorageProvider` interface
- [ ] `type` property is `'s3'` (readonly)
- [ ] Constructor takes `S3ProviderConfig`, creates `S3Client` with region
- [ ] `upload()` uses `@aws-sdk/lib-storage` `Upload` for streaming (NFR-005) — NOT `PutObjectCommand` for streams
- [ ] `upload()` applies SSE-S3 (`AES256`) by default, SSE-KMS when configured (D5)
- [ ] `download()` uses `GetObjectCommand`, returns `response.Body` as `Readable`
- [ ] `download()` returns `STORAGE_ERRORS.NOT_FOUND` when body is null
- [ ] `delete()` uses `DeleteObjectCommand`
- [ ] `getSignedUrl()` uses `@aws-sdk/s3-request-presigner` `getSignedUrl` with `expiresIn`
- [ ] `exists()` uses `HeadObjectCommand`, returns `false` on error (not an AppError)
- [ ] All methods return `AsyncResult<T, AppError>` — no thrown exceptions
- [ ] Error codes match `STORAGE_ERRORS` constants
- [ ] No `any` types
- [ ] All tests pass (mocked AWS SDK, minimum 8 tests: upload buffer, upload stream with SSE, download, download not found, delete, getSignedUrl, exists true/false, KMS encryption)

---

## Task 5: DriveProvider

**Priority:** P2  
**Estimated time:** 15 min  
**FR:** FR-001, FR-006  
**Dependencies:** Task 0, Task 1

**Files to create:**

- `packages/storage-engine/src/providers/drive.provider.ts`

**Test file:** `packages/storage-engine/tests/unit/drive-provider.test.ts`

**Acceptance criteria:**

- [ ] `DriveProvider` class implements `StorageProvider` interface
- [ ] `type` property is `'drive'` (readonly)
- [ ] Constructor takes pre-configured `drive_v3.Drive` client AND `DriveProviderConfig` — does NOT manage OAuth2 tokens
- [ ] `upload()` uses `driveClient.files.create()` with `requestBody.parents: [folderId]` and `media: { mimeType, body: data }`
- [ ] `upload()` returns `providerKey` as the Drive file ID and `url` as `webViewLink`
- [ ] `upload()` returns error when no file ID returned from Drive API
- [ ] `download()` uses `driveClient.files.get()` with `alt: 'media'` and `responseType: 'stream'`
- [ ] `delete()` uses `driveClient.files.delete()` with `fileId: key`
- [ ] `getSignedUrl()` uses `driveClient.files.get()` to retrieve `webViewLink`, errors when no link
- [ ] `exists()` uses `driveClient.files.get()` with `fields: 'id'`, returns `false` on error
- [ ] All methods return `AsyncResult<T, AppError>` — no thrown exceptions
- [ ] Error codes match `STORAGE_ERRORS` constants
- [ ] No `any` types — `drive_v3.Drive` is the typed client
- [ ] All tests pass (mocked Drive client, minimum 7 tests: upload, upload no ID, download, delete, getSignedUrl, getSignedUrl no link, exists true/false)

---

## Task 6: StorageProviderFactory

**Priority:** P1  
**Estimated time:** 5 min  
**FR:** FR-001, D1, NFR-004  
**Dependencies:** Task 3, Task 4, Task 5

**Files to create:**

- `packages/storage-engine/src/provider.factory.ts`

**Test file:** `packages/storage-engine/tests/unit/provider-factory.test.ts`

**Acceptance criteria:**

- [ ] `createStorageProvider(config: StorageConfig)` function exported, returns `Result<StorageProvider, AppError>`
- [ ] Creates `LocalProvider` when `config.provider === 'local'` and `config.local` exists
- [ ] Creates `S3Provider` when `config.provider === 's3'` and `config.s3` exists
- [ ] Returns error with `STORAGE_ERRORS.PROVIDER_UNKNOWN` for `'drive'` (must use `createDriveProvider()`)
- [ ] Returns error with `STORAGE_ERRORS.PROVIDER_UNKNOWN` for missing provider config
- [ ] Returns error with `STORAGE_ERRORS.PROVIDER_UNKNOWN` for unknown provider type
- [ ] `createDriveProvider(driveClient, config)` function exported, returns `Result<StorageProvider, AppError>`
- [ ] No `any` types
- [ ] All tests pass (minimum 8 tests: local success, local missing config, s3 success, s3 missing config, drive via createStorageProvider, drive via createDriveProvider, telegram via createStorageProvider, telegram via createTelegramProvider, unknown provider)

---

## Task 7: Attachment Prisma Model

**Priority:** P1 (cross-package modification)  
**Estimated time:** 5 min  
**FR:** FR-003  
**Dependencies:** Task 0

**Files to modify:**

- `packages/database/prisma/schema.prisma`

**Test file:** N/A (Prisma model — validated by `prisma format` / type generation)

**Acceptance criteria:**

- [ ] `Attachment` model added to schema with ALL fields from spec: `id`, `fileName`, `originalName`, `mimeType`, `size`, `provider`, `providerKey` (unique), `url?`, `metadata?` (Json), `moduleId?`, `entityId?`, `isEncrypted`
- [ ] ALL BaseEntity audit fields present: `createdAt`, `updatedAt`, `createdBy?`, `updatedBy?`, `isDeleted`, `deletedAt?`, `deletedBy?`
- [ ] `id` uses `@id @default(cuid())`
- [ ] `providerKey` has `@unique` constraint
- [ ] `isEncrypted` defaults to `false`
- [ ] `isDeleted` defaults to `false`
- [ ] Indexes: `@@index([moduleId, entityId])`, `@@index([provider])`, `@@index([isDeleted])`
- [ ] No other files in `packages/database/` are modified
- [ ] `prisma format` passes without errors

---

## Task 8: AttachmentRepository

**Priority:** P1  
**Estimated time:** 10 min  
**FR:** FR-003, Rule XIV  
**Dependencies:** Task 1, Task 7

**Files to create:**

- `packages/storage-engine/src/attachment.repository.ts`

**Test file:** `packages/storage-engine/tests/unit/attachment-repository.test.ts`

**Acceptance criteria:**

- [ ] `AttachmentRepository` class extends `BaseRepository<Attachment>` from `@tempot/database`
- [ ] `protected moduleName = 'storage'`
- [ ] `protected entityName = 'attachment'`
- [ ] `protected get model()` returns `this.db.attachment` as `PrismaModelDelegate`
- [ ] `findByModuleAndEntity(moduleId, entityId)` returns `AsyncResult<Attachment[], AppError>` via inherited `findMany()`
- [ ] `findExpiredDeleted(beforeDate: Date)` returns `AsyncResult<Attachment[], AppError>` — queries `isDeleted: true, deletedAt: { lte: beforeDate }`
- [ ] `hardDelete(ids: string[])` returns `AsyncResult<void, AppError>` — uses `$executeRaw` to permanently remove records (bypasses soft-delete extension)
- [ ] Constructor signature matches `BaseRepository`: `(auditLogger: IAuditLogger, db?: DatabaseClient)`
- [ ] No direct Prisma calls except `$executeRaw` in `hardDelete()` — all other methods use inherited `findMany()`, `create()`, `findById()`, etc.
- [ ] No `any` types (except `PrismaModelDelegate` accessor which is allowed per base pattern)
- [ ] All tests pass (mocked Prisma delegate, minimum 5 tests: findByModuleAndEntity, findExpiredDeleted, create, findById, hardDelete)

---

## Task 9: StorageService (Orchestrator)

**Priority:** P1  
**Estimated time:** 20 min  
**FR:** FR-001 through FR-007, D3, SC-001  
**Dependencies:** Task 2, Task 6, Task 8

**Files to create:**

- `packages/storage-engine/src/storage.service.ts`

**Test file:** `packages/storage-engine/tests/unit/storage-service.test.ts`

**Acceptance criteria:**

- [ ] `StorageService` class exported
- [ ] `StorageServiceDeps` interface exported (groups 5 dependencies: provider, attachmentRepo, validation, eventBus, logger — Rule II compliance)
- [ ] Constructor takes `StorageServiceDeps` object
- [ ] `upload(data, options)` returns `AsyncResult<Attachment, AppError>`
- [ ] Upload flow: validate → MIME magic byte check (if Buffer) → generate provider key → upload to provider → create DB record → emit event → return attachment
- [ ] Upload generates correct path structure: `{moduleId ?? 'general'}/{YYYY-MM}/{uuidv7}.{ext}`
- [ ] Upload calls `validation.validateMimeType()` for Buffer data before uploading (Edge Case: MIME spoofing)
- [ ] Upload emits `storage.file.uploaded` via fire-and-log pattern: checks `eventBus.publish()` result, logs warning on failure, still returns success
- [ ] Upload sets `isEncrypted` based on provider type: `true` for S3/Drive/Telegram, `false` for Local (D5)
- [ ] Two-phase rollback (D3): if DB insert fails, checks rollback result and logs `STORAGE_ERRORS.ROLLBACK_FAILED` on cleanup failure
- [ ] `download(attachmentId)` returns `AsyncResult<Readable, AppError>` — looks up attachment then calls provider
- [ ] `delete(attachmentId)` returns `AsyncResult<void, AppError>` — looks up attachment, soft-deletes, then emits `storage.file.deleted` with `permanent: false` via fire-and-log pattern
- [ ] `getSignedUrl(attachmentId, expiresInSeconds?)` returns `AsyncResult<string, AppError>` — default 3600 seconds
- [ ] `findByModuleAndEntity(moduleId, entityId)` returns `AsyncResult<Attachment[], AppError>`
- [ ] All public methods return `AsyncResult<T, AppError>` — no thrown exceptions
- [ ] No `any` types
- [ ] All tests pass (mocked provider + repo + validation + eventBus + logger, minimum 15 tests: upload success with event, upload validation failure, upload MIME mismatch failure, upload provider failure, upload DB failure with rollback logged, upload rollback success not logged, upload event publish failure logged, upload isEncrypted for S3, download success, download not found, delete with event, delete event failure logged, getSignedUrl, findByModuleAndEntity, path generation)

---

## Task 10: Event Registration

**Priority:** P1 (cross-package modification)  
**Estimated time:** 5 min  
**FR:** FR-007  
**Dependencies:** Task 1

**Files to modify:**

- `packages/event-bus/src/events.ts`

**Test file:** N/A (type-level change — validated by TypeScript compilation)

**Acceptance criteria:**

- [ ] `TempotEvents` interface updated with `'storage.file.uploaded'` event and inline payload type
- [ ] `TempotEvents` interface updated with `'storage.file.deleted'` event and inline payload type
- [ ] Payload types defined **inline** in events.ts — do NOT import from `@tempot/storage-engine` (avoids circular dependency)
- [ ] Inline payload structures must match `StorageFileUploadedPayload` and `StorageFileDeletedPayload` from `contracts.ts`
- [ ] Event names follow naming convention: `{module}.{entity}.{action}` pattern validated by regex
- [ ] No other files in `packages/event-bus/` are modified
- [ ] TypeScript compilation passes with no errors

---

## Task 11: Deferred Deletion Job (Purge)

**Priority:** P2  
**Estimated time:** 10 min  
**FR:** FR-005, D6  
**Dependencies:** Task 8, Task 6

**Files to create:**

- `packages/storage-engine/src/jobs/purge.job.ts`

**Test file:** `packages/storage-engine/tests/unit/purge-job.test.ts`

**Acceptance criteria:**

- [ ] Uses `queueFactory('storage-purge')` from `@tempot/shared` to create BullMQ queue
- [ ] `createPurgeQueue` accepts optional `ShutdownManager` parameter for graceful shutdown (Rule XVII)
- [ ] Worker processes jobs by: querying `AttachmentRepository.findExpiredDeleted(beforeDate)` where `beforeDate = now - retentionDays`
- [ ] For each expired record: calls `provider.delete(providerKey)` then permanently removes DB record
- [ ] Emits `storage.file.deleted` with `permanent: true` for each permanently deleted record (FR-007, D6)
- [ ] Handles provider deletion failures gracefully (logs and continues with next record)
- [ ] Uses `@tempot/logger` for logging — no `console.*`
- [ ] Configurable retention period via `RetentionConfig.days` (default 30)
- [ ] Orphan cleanup (files without DB records) is NOT in scope — deferred to follow-up task
- [ ] All methods return `AsyncResult<T, AppError>` where applicable
- [ ] No `any` types
- [ ] All tests pass (mocked queue, repo, provider, eventBus, minimum 5 tests: purge with expired records, purge with no expired records, provider delete failure continues, event emission with permanent:true, queue creation)

---

## Task 12: Barrel Exports (`src/index.ts`)

**Priority:** P1  
**Estimated time:** 5 min  
**FR:** All (final integration)  
**Dependencies:** Task 1, 2, 3, 4, 5, 6, 8, 9, 10, 11

**Files to update:**

- `packages/storage-engine/src/index.ts`

**Test file:** All existing tests continue to pass

**Acceptance criteria:**

- [ ] Exports all types: `StorageProviderType`, `StorageConfig`, `LocalProviderConfig`, `S3ProviderConfig`, `DriveProviderConfig`, `TelegramProviderConfig`, `RetentionConfig`, `UploadOptions`, `ProviderUploadResult`, `Attachment`, `VectorIndexer`
- [ ] Exports `DEFAULT_STORAGE_CONFIG` constant
- [ ] Exports `StorageProvider` interface
- [ ] Exports event payloads: `StorageFileUploadedPayload`, `StorageFileDeletedPayload`
- [ ] Exports `STORAGE_ERRORS` constant
- [ ] Exports `ValidationService` and `ValidatedFile`
- [ ] Exports all providers: `LocalProvider`, `S3Provider`, `DriveProvider`, `TelegramProvider`
- [ ] Exports factory functions: `createStorageProvider`, `createDriveProvider`, `createTelegramProvider`
- [ ] Exports `AttachmentRepository`
- [ ] Exports `StorageService`
- [ ] All relative imports use `.js` extensions (ESM/NodeNext compliance)
- [ ] All existing tests still pass after barrel update
- [ ] 10-point package-creation-checklist passes final verification
- [ ] No `any` types in any file across the package
- [ ] No `console.*` in any file across the package

---

## Task Dependency Graph

```
Task 0 (scaffolding)
  ├─→ Task 1 (types/contracts/errors)
  │     ├─→ Task 2 (ValidationService)         ─┐
  │     ├─→ Task 3 (LocalProvider)              ─┤
  │     ├─→ Task 4 (S3Provider)                 ─┤─→ Task 6 (factory)  ─┐
  │     ├─→ Task 5 (DriveProvider)              ─┘                      │
  │     └─→ Task 10 (event registration)                                │
  │                                                                     │
  ├─→ Task 7 (Prisma model)                                            │
  │     └─→ Task 8 (AttachmentRepository)  ─────────────────────────────┤
  │                                                                     │
  │                                         Task 2 + Task 6 + Task 8 ──┤
  │                                               └─→ Task 9 (StorageService)
  │                                                                     │
  │                                         Task 6 + Task 8 ───────────┤
  │                                               └─→ Task 11 (purge job)
  │                                                                     │
  └───────────────────────────────── All tasks ─→ Task 12 (barrel exports)
```

## Summary

| Task      | Name                    | Priority | Est. Time   | FR Coverage               |
| --------- | ----------------------- | -------- | ----------- | ------------------------- |
| 0         | Package Scaffolding     | P0       | 5 min       | Infrastructure            |
| 1         | Types/Contracts/Errors  | P0       | 10 min      | FR-001, FR-004, FR-007    |
| 2         | ValidationService       | P1       | 15 min      | FR-004, SC-004, MIME Edge |
| 3         | LocalProvider           | P1       | 15 min      | FR-001, FR-006            |
| 4         | S3Provider              | P1       | 15 min      | FR-001, FR-006, D5        |
| 5         | DriveProvider           | P2       | 15 min      | FR-001, FR-006            |
| 6         | ProviderFactory         | P1       | 5 min       | FR-001, D1, NFR-004       |
| 7         | Attachment Prisma Model | P1       | 5 min       | FR-003                    |
| 8         | AttachmentRepository    | P1       | 10 min      | FR-003, Rule XIV          |
| 9         | StorageService          | P1       | 25 min      | FR-001–FR-007, D3, D5     |
| 10        | Event Registration      | P1       | 5 min       | FR-007                    |
| 11        | Purge Job               | P2       | 10 min      | FR-005, FR-007, D6        |
| 12        | Barrel Exports          | P1       | 5 min       | All                       |
| **Total** |                         |          | **140 min** |                           |
