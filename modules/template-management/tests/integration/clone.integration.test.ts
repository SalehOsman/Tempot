import { describe, it } from 'vitest';

describe('Clone Integration', () => {
  it.todo('clones a published template into DRAFT with clonedFrom link');
  it.todo('increments usage count on source template');
  it.todo('auto-subscribes user to source template');
  it.todo('rejects clone of non-published template');
});
