import { describe, it } from 'vitest';

describe('Template CRUD Integration', () => {
  it.todo('creates a template and returns ok Result');
  it.todo('reads template by id');
  it.todo('updates template name and description');
  it.todo('soft-deletes template and excludes from queries');
  it.todo('returns not_found error for nonexistent id');
});
